<UserControl x:Class="WpfOlzServer.ScheduleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="White" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="ScheduleDataGrid" AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding Schedules}" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="날짜 및 시간" Binding="{Binding Date, StringFormat='yyyy-MM-dd HH:mm'}" Width="*"/>
                <DataGridTextColumn Header="장소" Binding="{Binding Location}" Width="2*"/>
                <DataGridTextColumn Header="참여 인원" Binding="{Binding Participants.Count}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="새 일정 추가" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddScheduleCommand}"/>
            <Button Content="선택한 일정 삭제" Margin="10,0,0,0" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding DeleteScheduleCommand}" CommandParameter="{Binding SelectedItem, ElementName=ScheduleDataGrid}"/>
        </StackPanel>
    </Grid>
</UserControl>